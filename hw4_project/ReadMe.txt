Program works under this condition:
1) You must create a variable ( e.g.a= 1 ) before using the ? for the inventory.

Any other operation i.e ( 1+1 = 2 followed by ?) will result in a segmentation fault when ? is entered. I am trying to hunt down and fix this bug.
